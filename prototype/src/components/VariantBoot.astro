---
// PROTOTYPE for plank/docs#19: picks the dark palette before first paint. ?variant= is the source of
// truth; sessionStorage carries it across links that drop it, and the URL is rewritten so it stays shareable.
// The layouts are fixed to the ones chosen in plank/docs#7: landing page B, doc page A.
---

<script is:inline>
	(() => {
		const root = document.documentElement;
		root.dataset.layout = location.pathname === '/' ? 'B' : 'A';
		const keys = ['A', 'B', 'C'];
		const url = new URL(location.href);
		let v = (url.searchParams.get('variant') || '').toUpperCase();
		if (!keys.includes(v)) {
			try { v = sessionStorage.getItem('proto-dark') || ''; } catch {}
		}
		if (!keys.includes(v)) v = 'B'; // Ink, the User's pick on 2026-09-13
		root.dataset.variant = v;
		try { sessionStorage.setItem('proto-dark', v); } catch {}
		if (url.searchParams.get('variant') !== v) {
			url.searchParams.set('variant', v);
			history.replaceState(null, '', url);
		}
	})();
</script>
